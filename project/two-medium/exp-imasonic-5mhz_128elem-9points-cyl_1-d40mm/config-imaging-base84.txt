
saved_acquisition_dir = ../saved_acquisition/cyl_1/d40mm/9points

output_dir = output

image_dir = output-base84

#
upsampling_factor = 4

# The excitation waveform's peak is reached at peakOffset periods (center freq.) after the start time.
peak_offset = 3.03

# Number of wavelengths (at f_c).
max_fermat_block_size = 10

#
final_image_base_element = 84

# (m)
normalization_min_z = 50.0e-3

# One wavelength divided by this number (at f_c).
interface_step_div = 4.0

# (degree)
interface_angle_1 = -100

# (degree)
interface_angle_2 = -40
